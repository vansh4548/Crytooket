#spring.application.name=crytoket-backend
#spring.datasource.url=jdbc:mysql://localhost:3306/crytoket_db?createDatabaseIfNotExist=true
#spring.datasource.username=root
#spring.datasource.password=7017
# Show SQL statements in the console for debugging
#spring.jpa.show-sql=true
#spring.jpa.hibernate.ddl-auto=update


# Spring Mail Properties for Gmail
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=vanshgarg7017@gmail.com
#spring.mail.password=isan qdhk dxvi fmpp
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true

# Specify the SQL dialect for MySQL
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#jwt.secret.key=yourSuperSecretLongAndRandomKeyThatIsAtLeast256BitsLongAndSecure
#jwt.expiration.ms=86400000


spring.application.name=crytoket-backend

# Database (Aiven MySQL)
spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslMode=REQUIRED
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}

# Hibernate/JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Mail (secured with env vars)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# JWT
jwt.secret.key=${JWT_SECRET}
jwt.expiration.ms=86400000
